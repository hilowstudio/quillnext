# Quill & Compass — environment variables
# Copy to `.env` and fill in. Do NOT commit a populated `.env`.
# Key list verified against the codebase (process.env.* usage); placeholder values only.

# --- Database (PostgreSQL / Supabase) ---
# DATABASE_URL is the pooled/runtime connection; DIRECT_DATABASE_URL is the
# direct (non-pooled) connection used for migrations/seeds. Prisma resolves
# DIRECT_DATABASE_URL ?? DATABASE_URL (see prisma.config.ts).
#
# COMPAT: the Vercel↔Supabase integration injects DIFFERENT names for the same DB —
# POSTGRES_URL / POSTGRES_PRISMA_URL (pooled) and POSTGRES_URL_NON_POOLING (direct).
# The code falls back to those when DATABASE_URL / DIRECT_DATABASE_URL are unset
# (db.ts runtime; prisma.config.ts migrations). So on Vercel you can EITHER set
# DATABASE_URL/DIRECT_DATABASE_URL, OR rely on the integration's POSTGRES_* vars —
# but do NOT leave a STALE DATABASE_URL pointing at a different project (it wins).
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/postgres"
DIRECT_DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/postgres"
# RLS is OFF by default in the app layer; leave false unless you know why.
RLS_ENABLED="false"
# Optional Prisma engine override; leave unset unless required.
# PRISMA_CLIENT_ENGINE_TYPE=""

# --- Auth (NextAuth v5 + Google OAuth) ---
# AUTH_SECRET / NEXTAUTH_SECRET: random 32+ byte secret (e.g. `openssl rand -base64 32`).
AUTH_SECRET=""
NEXTAUTH_SECRET=""
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# --- AI (Vercel AI SDK) ---
# Google Gemini is the primary provider.
GOOGLE_GENERATIVE_AI_API_KEY=""
GEMINI_API_KEY=""

# --- Firebase Admin (server-side storage) ---
FIREBASE_PROJECT_ID=""
FIREBASE_CLIENT_EMAIL=""
# Paste the private key with literal \n newlines, quoted.
FIREBASE_PRIVATE_KEY=""
FIREBASE_STORAGE_BUCKET=""

# --- Email (Resend) ---
RESEND_API_KEY=""
# From-address for child-safety alert emails.
SAFETY_ALERT_FROM=""
# From-address for account/profile transactional emails (e.g. parent-PIN reset). Falls back to
# Resend's test sender (onboarding@resend.dev) when unset, which only delivers to the Resend owner.
ACCOUNT_EMAIL_FROM=""
# Where /waitlist signups are emailed. Falls back to adam@quillandcompass.app when unset.
WAITLIST_NOTIFY_TO=""
# From-address for waitlist emails. Falls back to SAFETY_ALERT_FROM, then Resend's test sender.
WAITLIST_FROM=""

# --- Third-party content APIs ---
YOUTUBE_API_KEY=""
GOOGLE_BOOKS_API_KEY=""
NEXT_PUBLIC_GOOGLE_BOOKS_API_KEY=""
JOSHUA_PROJECT_API_KEY=""
BIBLE_API_KEY=""

# --- Hi-Low ingest integration ---
HILOW_INGEST_URL=""
HILOW_INGEST_KEY=""

# Note: NODE_ENV and VERCEL / VERCEL_ENV / VERCEL_GIT_COMMIT_SHA are provided by
# the runtime/platform and should not be set here.
